----inodes----

int add_entry(int indexNode, char name[], bool ifFile);
bool remove_entry(int indexNode, char name[], bool ifFile);

static int get_next_entry(int indexNode);
static bool is_empty(int indexNode);

int getEntry(int indexNode, char name[]);

---creat operations---

//returns -1 if parent path does not exist
//returns index node if parent path exists
int ifParentExists(char path[]);

